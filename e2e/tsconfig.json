{
  "//": "The e2e tree is Node code, not app code: it must not inherit the app's es5/DOM-only target, and the app's type-aware lint must not sweep it in. Playwright compiles the specs itself; this config exists so editors and `npx tsc --noEmit -p e2e/tsconfig.json` typecheck them sanely.",
  "compilerOptions": {
    "target": "es2022",
    "lib": ["es2022", "dom"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": ["node"],
    "allowJs": true,
    "checkJs": false,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["**/*.ts", "../playwright.config.ts"]
}
