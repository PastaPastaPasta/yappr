Source: `/Users/pasta/workspace/platform-auth`

This directory is a vendored snapshot of the `platform-auth` package for portable builds.

Refresh process:

1. Rebuild `platform-auth`.
2. Sync the package contents into `vendor/platform-auth`.
3. Re-run `npm install --package-lock-only` in Yappr.
