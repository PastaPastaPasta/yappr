> Historical gap inventory at report b1371f3c. Follow-up testing is active; use the current QA_REPORT.md and PR evidence for newly completed cases.

# Remaining Yappr story matrix

Read-only source/evidence audit, 2026-09-15. Frozen source: `733faf53cd893cba861476a4af75b442ed67ca72` at `/Users/pasta/.t3/worktrees/yappr/qa-staging-733faf53`. Compared with root `QA_REPORT.md` at signed report `b1371f3c`, plus the specifically reviewed immutable artifacts identified below and in the subsidiary matrices. This inventory made no application mutations and confirmed no new defect by itself.

## Counts and limits

- Source has **38 route pages**. The report has **61 broad user-story rows**. Those rows aggregate many independent controls and transitions; they are not 61 complete end-to-end passes.
- This comparison identifies **63 concrete gap groups**: 16 social/account/settings/cross-cutting groups below, 23 commerce groups, and 24 blog/embed groups. Groups deliberately bundle related transitions and sometimes share fixtures. This count is a work list, not a new claim to cover every possible input combination.
- Existing simple lifecycle successes remain valid only for their recorded revisions/configurations. Do not repeat them merely because broader branches are untested; do run a final frozen integration pass after selected fixes are incorporated.
- No honest percentage-complete can be derived from the current report: rows mix visits, partial passes, confirmed defects, ongoing work, and blocked integrations. Record an executed result for each remaining transition and preserve its source hash before computing completion.
- “Gap” means no sufficiently specific evidence was found in the reviewed materials. Another agent's newer structured results may close a row; inspect those before repeating writes.

## Immediate allocation recommendations

1. **Bookmarks**: separate page-level search/sort/clear and per-card menu are unaccounted for. Source shows a separate copied-link expression that drops `/devnet`, but this requires normal browser confirmation. Root reserved **QA84**; coverage_inventory now owns interactive tests with isolated persona55 contexts, snapshot and changes-only restoration.
2. **Composer draft controls**: Escape/backdrop and Close use different cleanup paths; test opening a reply/quote/poll/NSFW draft, dismissing each way, and reopening normal composer. This is a source lead, not a confirmed issue. Also test formatting/preview/length/thread controls.
3. **Settings consumer behavior**: test notification preferences against real existing notifications, read-receipt preference with two controlled identities, media privacy against actual existing media, language against known-language posts. Toggling a switch and reloading alone does not prove the feature works.
4. **Commerce**: allocate discovery/cart and merchant/product/checkout bundles from the linked matrix. Protect the closed seller51 payment fixture and real paid order.
5. **Blog**: allocate reader/share/embed read-only work separately from a controlled owner draft/rich-text/theme round trip. URL images do not require a provider account.

Other agents already own private-feed recovery/status (QA81/82), reset routing (QA80), endpoint recovery (QA77), checkout PR465 review, notification QA74/75, payment-network QA76, profile layout/share QA78/79, and PR469 rebase. Do not duplicate those assignments.

## Source-backed social/account/settings gaps

Source references are relative to the frozen worktree. “Testable” means an existing public/dedicated synthetic fixture or ordinary local UI is sufficient; coordinate persona allocation before writes.

| ID | Priority / story | Missing ordinary interactive transitions and pass criteria | Source / prerequisites |
|---|---|---|---|
| S01 | P1 Composer drafts, formatting and dismissal | Enter selected text and use Bold/Italic/Code, keyboard Ctrl/Cmd+B/I, mention/hashtag insertion and emoji; Preview then return to edit; empty/whitespace/500/501-character state; Close/Escape/backdrop and reopen. Verify reply/quote context, preview, poll, NSFW and text reset consistently without publishing accidental content. | `components/compose/thread-post-editor.tsx:82`, `:101`, `:237`, `:328`; `components/compose/compose-modal.tsx:151`, `:176`, `:337`, `:347`; `lib/store.ts:67`; funded signed-in disposable context. |
| S02 | P2 Thread controls and retry | Add/remove/reorder-by-edit active cards through the exposed 10-post cap; remaining text/active focus and Preview; poll/private/reply/quote eligibility; Ctrl/Cmd+Enter intended submit; one bounded ordinary offline failure preserves draft and explicit retry does not duplicate any already-confirmed parts. Three-part successful publication already passed. | `components/compose/compose-modal.tsx:165`, `:300`, `:337`, `:560`; thread publish service; reserved owner/YAPP, disposable content only, fresh readback and cleanup. No stress workload. |
| S03 | P2 Poll compatibility branches | Existing evidence proves create/vote/add/cancel/expiry and option limits. Remaining normal UI: guest vote prompt, show-results/back-to-ballot behavior if exposed, single↔multiple draft state, remove poll and reopen composer, entering private/reply/quote/thread disables or removes incompatible poll consistently. Do not invent a backend expiry defect or submit intentionally invalid state. | `components/poll/poll-card.tsx`; `hooks/use-compose-poll.ts:36`; `components/compose/compose-modal.tsx:114`; existing poll fixture/read-only reader and disposable draft. |
| S04 | P1 Bookmark management | Existing one add/remove cycle does not cover page search by content/username, no match/clear, recent/oldest order, header Clear all Cancel/confirm, per-card Remove, copied-link destination, mobile/keyboard menu discoverability, failure retention and fresh readback. Snapshot existing set; add known public posts and remove only those. Clear-all confirmation only if initial set is empty or full restoration is explicitly verified. | `app/bookmarks/page.tsx:105`, `:129`, `:160`, `:182`, `:263`, `:277`; persona55 assigned, own isolated browser contexts. |
| S05 | P1 DM read receipts and unread state | Turn receipts off, receive a controlled message, open thread, verify local unread and sender-visible read state; reload/return; enable and open new controlled message, verify sender receives read state. Keep preference persistence distinct from actual protocol read state; initial messaging send/history/search tests already passed. | `app/messages/page.tsx:62`, `:246`, `:277`; `app/settings/page.tsx:415`; two reserved identities, no reuse of current DM markers. |
| S06 | P1 Notification preference consumption | Against existing real liked/reposted/replied/followed/mentioned/blog notifications, disable each relevant setting and verify row visibility, tab unread count and overall unread count; re-enable restores eligible notifications; refresh retains choices. Private-feed notifications intentionally remain visible. Test list type controls separately from these preferences. Current appearance artifact proves theme + reply mark-read only; later switch tests prove names/persistence, not all consumers. | `app/settings/page.tsx:327`; `app/notifications/page.tsx:170`–214; local settings context + existing real notification fixtures; coordinate QA74/75 owner. |
| S07 | P1 Media and link-preview privacy | With existing benign public media/URL posts: guest/non-followed gate, Show, remount/reload resets reveal; own/followed bypass, follow/unfollow while multiple cards mounted; disable gate; disable Link Previews stops preview display/fetch; enable restores. Confirm placeholder/fallback and external link navigation, light/dark/mobile and Potato Mode removes blur without breaking reveal. This is distinct from NSFW text gating and provider upload. | `hooks/use-media-gate.ts:37`; `components/post/gated-media.tsx:23`; `components/post/post-content.tsx:88`, `:465`; `app/settings/page.tsx:355`, `:537`; existing URL/media posts; no provider needed. |
| S08 | P1 Feed language | Select two languages with known existing tagged/detected-language posts, reload and verify For You includes correct fixture/excludes alternate-language fixture; return default. Check Following intentionally retains its own semantics. Accessible label/persistence evidence alone is insufficient. | `app/settings/page.tsx:555`; `hooks/use-feed-data.ts:266`, `:325`, `:434`; `lib/feed/load-for-you-feed.ts:95`; known-language public fixture, no new bulk writes. |
| S09 | P2 Ranking windows, cashtags and profile subviews | Existing All-time/Recent/Top pages passed. Reconcile actual topology test results for Today/All on feed, hashtag, Explore trending/top/creators and profile before rerunning. Exercise a known cashtag, source-supported search prefix, profile Mentions and Blog destinations, empty windows. A green job with continue-on-error is not proof. | `components/explore/ranking-window-toggle.tsx:40`; `app/explore/page.tsx:415`, `:442`, `:483`, `:512`; `components/profile/profile-tabs.tsx:72`, `:135`, `:155`; existing likes/tags/articles, current CI topology diagnosis already assigned. |
| S10 | P2 Profile fields, public payment QR and names | Bio/social/avatar are covered. Verify all ordinary text fields including website/pronouns/location clear/save/fresh readback; Cancel restores draft; profile NSFW interstitial and author-sensitive default; Payment Addresses add duplicate/remove/max and displayed QR Copy Address/Open Wallet URI; All Usernames/identity-copy correctness. Only use public QA addresses and restore original raw fields. QA78/79 own profile-header layout and share link. | `components/profile/profile-edit-form.tsx:49`; `components/profile/payment-uri-input.tsx:63`; `components/profile/profile-header.tsx:209`, `:218`, `:325`; `components/ui/usernames-modal.tsx:16`; `components/ui/payment-qr-code.tsx:104`; reserved profile fixture. Native wallet completion remains separate. |
| S11 | P2 Profile hover and engagement actions | Existing profile/engagement list reads passed. Hover/focus card Follow→Unfollow and Message/Profile destinations, self/guest variants, pointer/keyboard dismissal; engagement row Follow/Unfollow plus own-profile Likes modal. Restore original relationships. No general Report menu exists in the inspected post options. | `components/profile/profile-hover-card.tsx:146`, `:188`, `:197`; `components/post/post-card.tsx:209`, `:339`; `app/post/engagements/page.tsx:238`, `:270`; existing public identity and reserved viewer. |
| S12 | P1 Vault deletion and key prerequisites | Latest persona39 evidence explicitly says Delete Vault Access was NOT exercised. Use a separately allocated disposable vault with verified private-key fallback: Cancel delete, confirm deletion, fresh status and password/passkey non-restoration, key sign-in and re-enroll if required. Also reconcile ordinary malformed/wrong-identity key feedback and missing published encryption-key creation with recovery agent before extra work. Do not delete persona39 established vault. | `components/settings/key-backup-settings.tsx:200`, `:445`; `components/auth/add-encryption-key-modal.tsx:153`, `:237`; disposable identity/vault and secret-safe capture. Recovery QA81/82 owner should cover overlapping key branches. |
| S13 | P2 Wallet-entry UI independent of external ceremony | Real external wallet remains blocked, but QR Copy link (actual URI), touch Open in wallet link, close/reopen cancellation, ordinary request timeout/Check again and Advanced key entry are testable without pretending successful external login. Check visible mobile/keyboard flow. | `components/auth/key-exchange-qr.tsx:28`, `:38`, `:50`; `components/auth/wallet-login-panel.tsx:72`; fresh guest context; no fabricated QR response. |
| S14 | P2 DashPay contacts | Existing empty discovery is covered. Loading failure/retry can be checked with ordinary offline toggle. Populated mutual contacts, already-followed count, Follow one, Follow all partial failure/retry, dismissal after bulk completion need a controlled mutual DashPay contact fixture; search found none. Absence of a wallet simulator alone does not prove no safe contact fixture can be provisioned, but no existing fixture is identified. | `components/contacts/dashpay-contacts-modal.tsx:37`, `:57`, `:79`, `:140`; contact service. Existing dedicated populated DashPay contacts fixture or approved ordinary supported creation required. |
| S15 | P2 Authority-only moderation | Missing from broad report inventory: ordinary nonauthority cannot see/open Moderation; authority form has Freeze/Unfreeze/Slash and optional public note. Gate read check is testable. Full supported authority workflow requires dedicated YAPP authority fixture plus a disposable funded subject; slashing is irreversible and must not touch existing audit balances. Do not use a forged local session as proof of authority behavior. | `app/settings/page.tsx:98`, `:102`, moderation render gate; `components/settings/moderation-settings.tsx:16`, `:22`, `:84`; authority fixture not located by this read-only task. |
| S16 | P2 Navigation, guest boundaries and final integration | Reconcile protected-route guest prompts/return destinations, settings deep-link invalid section/back/history, empty/missing/nonexistent public object routes, mobile menu transitions and dialog focus/dismissal for the newly tested flows. Existing selected a11y PR evidence is not a whole-app certification. Run a current frozen integration smoke using About hash and actual route data after fixes are incorporated, including key login/public feed/bookmark/message/blog/store/cart/order reads. | 38 app routes; `app/settings/page.tsx:121`; layout/dialog components; guest and reserved signed-in contexts. Cross-browser/physical-device claims require those actual environments. |

## Commerce and blog detail

- `commerce-gaps.md`: **23 objective groups**, with exact source references, reviewed immutable evidence and four executable bundles. 22 groups contain ordinary feasible UI branches; successful provider upload and native wallet integration remain fixture-dependent within/alongside them. This does not mean 22 entirely untested stories: most are missing branches of a proven simple lifecycle.
- `blog-gaps.md`: **24 gap groups**, exact source references, evidence scope and fixture prerequisites. Normal rich editor blocks, URL media, reader preferences, theme controls, drafts, share/quote, embed variants and comments error handling remain testable. Coordinate blog notification work already in progress.

## Explicit report corrections

1. Remove “Native share sheet remains” from the post sharing row. A source search across `app`, `components` and `hooks` finds no `navigator.share`/`canShare`; the implemented post Share is clipboard-only at `components/post/post-card.tsx:240`. This is an absent feature, not a failed or fixture-blocked flow.
2. Do not add a generic post Report story: current post menu exposes Follow/Unfollow, View engagements, own Delete and Block/Unblock. There is no Report action in normal UI inspected here.
3. Ordinary post Edit and article Delete are not exposed; do not fabricate UI tests or direct service mutations for nonexistent actions. Blog drafts are local autosave, not a server-side publish-later queue.
4. Notification settings are consumed as in-app type visibility/unread preferences in this source. The heading “Push Notifications” does not establish an OS/browser push subscription or external notification delivery implementation.
5. Add authority moderation to the inventory with its fixture limitation. Do not silently count an authority-only story as passed under a regular account.
6. Keep SDK checkout payment distinct from actual wallet GUI/deep-link interoperability; keep image URL preview/save distinct from real provider upload.

## External fixture limitations

Safe searched fixture inventory: `/tmp/yappr-wallet-fixture-availability.md`. Historical device names/UUIDs are not usable configured wallets; two surviving app bundles carry no wallet data, and current local DashConnect implementations are testnet-only. No dedicated QA Pinata/Storacha account was found. Do not repeat the transcript search absent new lead information.

Provider-dependent success branches span post pick/paste, avatar/banner uploads, store branding, blog cover/inline upload, and provider connect/disconnect/exclusivity. Validate ordinary no-provider/empty/rejected-file UI now where reachable; keep successful upload/readback uncertified until a provider fixture is genuinely available. Physical passkey/cross-browser/device interoperability is distinct from the passed Chromium virtual-authenticator ceremony.

## Evidence scope checked directly in this inventory

In addition to root report and the subsidiary sources, immutable `settings-notifications/README.md` at `204a49f868720375af2c7975771a73f1a0df4dc7` proves Light/Dark/System and one reply-notification mark/read/navigation cycle. `key-session-51/README.md` at `d0fe755d96e304057df5a6e57d071b0f5a396e65` proves actual HIGH/CRITICAL key login, local encryption restore, disk restart, logout and nonexistent username rejection. `auth-vault-persona39-current/README.md` at `7d938bf803b1e6a45ed1b219c050fbed4a5b901a` proves wrong/correct password, key-login locked state, fresh virtual passkey and reload/logout; explicitly excludes Delete Vault Access and physical/cross-browser ceremonies.

These direct reads close overly broad assumptions but do not replace executing the remaining tests. No exhaustive-completion claim is supported yet.
